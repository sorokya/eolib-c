// This file is automatically generated by tools/code_gen.c
// Please do not edit it directly

#include "../include/protocol.h"
#include "../include/data.h"
#include <stdint.h>
#include <stdlib.h>
#include <string.h>

static uint8_t g_roundtrip_buf[65536];
static int g_roundtrip_len = 0;

int wasm_get_roundtrip_len(void) { return g_roundtrip_len; }

const uint8_t *wasm_packet_roundtrip(
    int family, int action, int direction,
    const uint8_t *input, int input_len)
{
    g_roundtrip_len = 0;
    EoReader reader = eo_reader_init(
        input_len > 0 ? input : NULL,
        (size_t)(input_len > 0 ? input_len : 0));
    EoWriter writer = eo_writer_init_with_capacity(
        (size_t)(input_len > 0 ? input_len : 1));
    int result = 0;

    switch (family) {
        case PACKET_FAMILY_INIT:
            switch (action) {
                case PACKET_ACTION_INIT:
                    if (direction == 0) {
                        InitInitClientPacket pkt;
                        result = InitInitClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = InitInitClientPacket_serialize(&pkt, &writer);
                            InitInitClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        InitInitServerPacket pkt;
                        result = InitInitServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = InitInitServerPacket_serialize(&pkt, &writer);
                            InitInitServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_CONNECTION:
            switch (action) {
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        ConnectionAcceptClientPacket pkt;
                        result = ConnectionAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ConnectionAcceptClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PING:
                    if (direction == 0) {
                        result = ConnectionPingClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = ConnectionPingClientPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        ConnectionPlayerServerPacket pkt;
                        result = ConnectionPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ConnectionPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_ACCOUNT:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        AccountRequestClientPacket pkt;
                        result = AccountRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AccountRequestClientPacket_serialize(&pkt, &writer);
                            AccountRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CREATE:
                    if (direction == 0) {
                        AccountCreateClientPacket pkt;
                        result = AccountCreateClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AccountCreateClientPacket_serialize(&pkt, &writer);
                            AccountCreateClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 0) {
                        AccountAgreeClientPacket pkt;
                        result = AccountAgreeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AccountAgreeClientPacket_serialize(&pkt, &writer);
                            AccountAgreeClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        AccountReplyServerPacket pkt;
                        result = AccountReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AccountReplyServerPacket_serialize(&pkt, &writer);
                            AccountReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_CHARACTER:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        CharacterRequestClientPacket pkt;
                        result = CharacterRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CharacterRequestClientPacket_serialize(&pkt, &writer);
                            CharacterRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CREATE:
                    if (direction == 0) {
                        CharacterCreateClientPacket pkt;
                        result = CharacterCreateClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CharacterCreateClientPacket_serialize(&pkt, &writer);
                            CharacterCreateClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        CharacterTakeClientPacket pkt;
                        result = CharacterTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CharacterTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        CharacterRemoveClientPacket pkt;
                        result = CharacterRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CharacterRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        CharacterReplyServerPacket pkt;
                        result = CharacterReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CharacterReplyServerPacket_serialize(&pkt, &writer);
                            CharacterReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        CharacterPlayerServerPacket pkt;
                        result = CharacterPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CharacterPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_LOGIN:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        LoginRequestClientPacket pkt;
                        result = LoginRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LoginRequestClientPacket_serialize(&pkt, &writer);
                            LoginRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        LoginReplyServerPacket pkt;
                        result = LoginReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LoginReplyServerPacket_serialize(&pkt, &writer);
                            LoginReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_WELCOME:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        WelcomeRequestClientPacket pkt;
                        result = WelcomeRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomeRequestClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_MSG:
                    if (direction == 0) {
                        WelcomeMsgClientPacket pkt;
                        result = WelcomeMsgClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomeMsgClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 0) {
                        WelcomeAgreeClientPacket pkt;
                        result = WelcomeAgreeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomeAgreeClientPacket_serialize(&pkt, &writer);
                            WelcomeAgreeClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PING:
                    if (direction == 1) {
                        WelcomePingServerPacket pkt;
                        result = WelcomePingServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomePingServerPacket_serialize(&pkt, &writer);
                            WelcomePingServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PONG:
                    if (direction == 1) {
                        WelcomePongServerPacket pkt;
                        result = WelcomePongServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomePongServerPacket_serialize(&pkt, &writer);
                            WelcomePongServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_NET242:
                    if (direction == 1) {
                        WelcomeNet242ServerPacket pkt;
                        result = WelcomeNet242ServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomeNet242ServerPacket_serialize(&pkt, &writer);
                            WelcomeNet242ServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_NET243:
                    if (direction == 1) {
                        WelcomeNet243ServerPacket pkt;
                        result = WelcomeNet243ServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomeNet243ServerPacket_serialize(&pkt, &writer);
                            WelcomeNet243ServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_NET244:
                    if (direction == 1) {
                        WelcomeNet244ServerPacket pkt;
                        result = WelcomeNet244ServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomeNet244ServerPacket_serialize(&pkt, &writer);
                            WelcomeNet244ServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        WelcomeReplyServerPacket pkt;
                        result = WelcomeReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WelcomeReplyServerPacket_serialize(&pkt, &writer);
                            WelcomeReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_ADMIN_INTERACT:
            switch (action) {
                case PACKET_ACTION_TELL:
                    if (direction == 0) {
                        AdminInteractTellClientPacket pkt;
                        result = AdminInteractTellClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AdminInteractTellClientPacket_serialize(&pkt, &writer);
                            AdminInteractTellClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        AdminInteractTellServerPacket pkt;
                        result = AdminInteractTellServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AdminInteractTellServerPacket_serialize(&pkt, &writer);
                            AdminInteractTellServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPORT:
                    if (direction == 0) {
                        AdminInteractReportClientPacket pkt;
                        result = AdminInteractReportClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AdminInteractReportClientPacket_serialize(&pkt, &writer);
                            AdminInteractReportClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        AdminInteractReplyServerPacket pkt;
                        result = AdminInteractReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AdminInteractReplyServerPacket_serialize(&pkt, &writer);
                            AdminInteractReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 1) {
                        AdminInteractRemoveServerPacket pkt;
                        result = AdminInteractRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AdminInteractRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        AdminInteractAgreeServerPacket pkt;
                        result = AdminInteractAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AdminInteractAgreeServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_LIST:
                    if (direction == 1) {
                        AdminInteractListServerPacket pkt;
                        result = AdminInteractListServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AdminInteractListServerPacket_serialize(&pkt, &writer);
                            AdminInteractListServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_GLOBAL:
            switch (action) {
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        result = GlobalRemoveClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = GlobalRemoveClientPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 0) {
                        result = GlobalPlayerClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = GlobalPlayerClientPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        result = GlobalOpenClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = GlobalOpenClientPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 0) {
                        result = GlobalCloseClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = GlobalCloseClientPacket_serialize(&writer);
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_TALK:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        TalkRequestClientPacket pkt;
                        result = TalkRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkRequestClientPacket_serialize(&pkt, &writer);
                            TalkRequestClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        TalkRequestServerPacket pkt;
                        result = TalkRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkRequestServerPacket_serialize(&pkt, &writer);
                            TalkRequestServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        TalkOpenClientPacket pkt;
                        result = TalkOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkOpenClientPacket_serialize(&pkt, &writer);
                            TalkOpenClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        TalkOpenServerPacket pkt;
                        result = TalkOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkOpenServerPacket_serialize(&pkt, &writer);
                            TalkOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_MSG:
                    if (direction == 0) {
                        TalkMsgClientPacket pkt;
                        result = TalkMsgClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkMsgClientPacket_serialize(&pkt, &writer);
                            TalkMsgClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        TalkMsgServerPacket pkt;
                        result = TalkMsgServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkMsgServerPacket_serialize(&pkt, &writer);
                            TalkMsgServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_TELL:
                    if (direction == 0) {
                        TalkTellClientPacket pkt;
                        result = TalkTellClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkTellClientPacket_serialize(&pkt, &writer);
                            TalkTellClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        TalkTellServerPacket pkt;
                        result = TalkTellServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkTellServerPacket_serialize(&pkt, &writer);
                            TalkTellServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPORT:
                    if (direction == 0) {
                        TalkReportClientPacket pkt;
                        result = TalkReportClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkReportClientPacket_serialize(&pkt, &writer);
                            TalkReportClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 0) {
                        TalkPlayerClientPacket pkt;
                        result = TalkPlayerClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkPlayerClientPacket_serialize(&pkt, &writer);
                            TalkPlayerClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        TalkPlayerServerPacket pkt;
                        result = TalkPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkPlayerServerPacket_serialize(&pkt, &writer);
                            TalkPlayerServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        TalkUseClientPacket pkt;
                        result = TalkUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkUseClientPacket_serialize(&pkt, &writer);
                            TalkUseClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADMIN:
                    if (direction == 0) {
                        TalkAdminClientPacket pkt;
                        result = TalkAdminClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkAdminClientPacket_serialize(&pkt, &writer);
                            TalkAdminClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        TalkAdminServerPacket pkt;
                        result = TalkAdminServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkAdminServerPacket_serialize(&pkt, &writer);
                            TalkAdminServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ANNOUNCE:
                    if (direction == 0) {
                        TalkAnnounceClientPacket pkt;
                        result = TalkAnnounceClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkAnnounceClientPacket_serialize(&pkt, &writer);
                            TalkAnnounceClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        TalkAnnounceServerPacket pkt;
                        result = TalkAnnounceServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkAnnounceServerPacket_serialize(&pkt, &writer);
                            TalkAnnounceServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        TalkReplyServerPacket pkt;
                        result = TalkReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkReplyServerPacket_serialize(&pkt, &writer);
                            TalkReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_SERVER:
                    if (direction == 1) {
                        TalkServerServerPacket pkt;
                        result = TalkServerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkServerServerPacket_serialize(&pkt, &writer);
                            TalkServerServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_LIST:
                    if (direction == 1) {
                        TalkListServerPacket pkt;
                        result = TalkListServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkListServerPacket_serialize(&pkt, &writer);
                            TalkListServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        TalkSpecServerPacket pkt;
                        result = TalkSpecServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TalkSpecServerPacket_serialize(&pkt, &writer);
                            TalkSpecServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_ATTACK:
            switch (action) {
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        AttackUseClientPacket pkt;
                        result = AttackUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AttackUseClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        AttackPlayerServerPacket pkt;
                        result = AttackPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AttackPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ERROR:
                    if (direction == 1) {
                        result = AttackErrorServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = AttackErrorServerPacket_serialize(&writer);
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_CHAIR:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        ChairRequestClientPacket pkt;
                        result = ChairRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChairRequestClientPacket_serialize(&pkt, &writer);
                            ChairRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        ChairPlayerServerPacket pkt;
                        result = ChairPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChairPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        ChairReplyServerPacket pkt;
                        result = ChairReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChairReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 1) {
                        ChairCloseServerPacket pkt;
                        result = ChairCloseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChairCloseServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 1) {
                        ChairRemoveServerPacket pkt;
                        result = ChairRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChairRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_SIT:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        SitRequestClientPacket pkt;
                        result = SitRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SitRequestClientPacket_serialize(&pkt, &writer);
                            SitRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        SitPlayerServerPacket pkt;
                        result = SitPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SitPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 1) {
                        SitCloseServerPacket pkt;
                        result = SitCloseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SitCloseServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 1) {
                        SitRemoveServerPacket pkt;
                        result = SitRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SitRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        SitReplyServerPacket pkt;
                        result = SitReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SitReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_EMOTE:
            switch (action) {
                case PACKET_ACTION_REPORT:
                    if (direction == 0) {
                        EmoteReportClientPacket pkt;
                        result = EmoteReportClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EmoteReportClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        EmotePlayerServerPacket pkt;
                        result = EmotePlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EmotePlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_FACE:
            switch (action) {
                case PACKET_ACTION_PLAYER:
                    if (direction == 0) {
                        FacePlayerClientPacket pkt;
                        result = FacePlayerClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = FacePlayerClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        FacePlayerServerPacket pkt;
                        result = FacePlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = FacePlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_WALK:
            switch (action) {
                case PACKET_ACTION_ADMIN:
                    if (direction == 0) {
                        WalkAdminClientPacket pkt;
                        result = WalkAdminClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WalkAdminClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 0) {
                        WalkSpecClientPacket pkt;
                        result = WalkSpecClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WalkSpecClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 0) {
                        WalkPlayerClientPacket pkt;
                        result = WalkPlayerClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WalkPlayerClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        WalkPlayerServerPacket pkt;
                        result = WalkPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WalkPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        WalkReplyServerPacket pkt;
                        result = WalkReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WalkReplyServerPacket_serialize(&pkt, &writer);
                            WalkReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 1) {
                        result = WalkCloseServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = WalkCloseServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 1) {
                        result = WalkOpenServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = WalkOpenServerPacket_serialize(&writer);
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_BANK:
            switch (action) {
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        BankOpenClientPacket pkt;
                        result = BankOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BankOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        BankOpenServerPacket pkt;
                        result = BankOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BankOpenServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ADD:
                    if (direction == 0) {
                        BankAddClientPacket pkt;
                        result = BankAddClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BankAddClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        BankTakeClientPacket pkt;
                        result = BankTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BankTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        BankReplyServerPacket pkt;
                        result = BankReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BankReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_BARBER:
            switch (action) {
                case PACKET_ACTION_BUY:
                    if (direction == 0) {
                        BarberBuyClientPacket pkt;
                        result = BarberBuyClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BarberBuyClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        BarberOpenClientPacket pkt;
                        result = BarberOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BarberOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        BarberOpenServerPacket pkt;
                        result = BarberOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BarberOpenServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        BarberAgreeServerPacket pkt;
                        result = BarberAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BarberAgreeServerPacket_serialize(&pkt, &writer);
                            BarberAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_LOCKER:
            switch (action) {
                case PACKET_ACTION_ADD:
                    if (direction == 0) {
                        LockerAddClientPacket pkt;
                        result = LockerAddClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerAddClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        LockerTakeClientPacket pkt;
                        result = LockerTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        LockerOpenClientPacket pkt;
                        result = LockerOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        LockerOpenServerPacket pkt;
                        result = LockerOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerOpenServerPacket_serialize(&pkt, &writer);
                            LockerOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_BUY:
                    if (direction == 0) {
                        result = LockerBuyClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = LockerBuyClientPacket_serialize(&writer);
                    }
                    if (direction == 1) {
                        LockerBuyServerPacket pkt;
                        result = LockerBuyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerBuyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        LockerReplyServerPacket pkt;
                        result = LockerReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerReplyServerPacket_serialize(&pkt, &writer);
                            LockerReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_GET:
                    if (direction == 1) {
                        LockerGetServerPacket pkt;
                        result = LockerGetServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerGetServerPacket_serialize(&pkt, &writer);
                            LockerGetServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        LockerSpecServerPacket pkt;
                        result = LockerSpecServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = LockerSpecServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_CITIZEN:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        CitizenRequestClientPacket pkt;
                        result = CitizenRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenRequestClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        CitizenRequestServerPacket pkt;
                        result = CitizenRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenRequestServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        CitizenAcceptClientPacket pkt;
                        result = CitizenAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenAcceptClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        CitizenAcceptServerPacket pkt;
                        result = CitizenAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenAcceptServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 0) {
                        CitizenReplyClientPacket pkt;
                        result = CitizenReplyClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenReplyClientPacket_serialize(&pkt, &writer);
                            CitizenReplyClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        CitizenReplyServerPacket pkt;
                        result = CitizenReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        CitizenRemoveClientPacket pkt;
                        result = CitizenRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        CitizenRemoveServerPacket pkt;
                        result = CitizenRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        CitizenOpenClientPacket pkt;
                        result = CitizenOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        CitizenOpenServerPacket pkt;
                        result = CitizenOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CitizenOpenServerPacket_serialize(&pkt, &writer);
                            CitizenOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_SHOP:
            switch (action) {
                case PACKET_ACTION_CREATE:
                    if (direction == 0) {
                        ShopCreateClientPacket pkt;
                        result = ShopCreateClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopCreateClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ShopCreateServerPacket pkt;
                        result = ShopCreateServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopCreateServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_BUY:
                    if (direction == 0) {
                        ShopBuyClientPacket pkt;
                        result = ShopBuyClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopBuyClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ShopBuyServerPacket pkt;
                        result = ShopBuyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopBuyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_SELL:
                    if (direction == 0) {
                        ShopSellClientPacket pkt;
                        result = ShopSellClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopSellClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ShopSellServerPacket pkt;
                        result = ShopSellServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopSellServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        ShopOpenClientPacket pkt;
                        result = ShopOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ShopOpenServerPacket pkt;
                        result = ShopOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ShopOpenServerPacket_serialize(&pkt, &writer);
                            ShopOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_STAT_SKILL:
            switch (action) {
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        StatSkillOpenClientPacket pkt;
                        result = StatSkillOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        StatSkillOpenServerPacket pkt;
                        result = StatSkillOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillOpenServerPacket_serialize(&pkt, &writer);
                            StatSkillOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        StatSkillTakeClientPacket pkt;
                        result = StatSkillTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        StatSkillTakeServerPacket pkt;
                        result = StatSkillTakeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillTakeServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        StatSkillRemoveClientPacket pkt;
                        result = StatSkillRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        StatSkillRemoveServerPacket pkt;
                        result = StatSkillRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ADD:
                    if (direction == 0) {
                        StatSkillAddClientPacket pkt;
                        result = StatSkillAddClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillAddClientPacket_serialize(&pkt, &writer);
                            StatSkillAddClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_JUNK:
                    if (direction == 0) {
                        StatSkillJunkClientPacket pkt;
                        result = StatSkillJunkClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillJunkClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        StatSkillJunkServerPacket pkt;
                        result = StatSkillJunkServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillJunkServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        StatSkillReplyServerPacket pkt;
                        result = StatSkillReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillReplyServerPacket_serialize(&pkt, &writer);
                            StatSkillReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        StatSkillPlayerServerPacket pkt;
                        result = StatSkillPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 1) {
                        StatSkillAcceptServerPacket pkt;
                        result = StatSkillAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = StatSkillAcceptServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_ITEM:
            switch (action) {
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        ItemUseClientPacket pkt;
                        result = ItemUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemUseClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_DROP:
                    if (direction == 0) {
                        ItemDropClientPacket pkt;
                        result = ItemDropClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemDropClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ItemDropServerPacket pkt;
                        result = ItemDropServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemDropServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_JUNK:
                    if (direction == 0) {
                        ItemJunkClientPacket pkt;
                        result = ItemJunkClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemJunkClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ItemJunkServerPacket pkt;
                        result = ItemJunkServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemJunkServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_GET:
                    if (direction == 0) {
                        ItemGetClientPacket pkt;
                        result = ItemGetClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemGetClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ItemGetServerPacket pkt;
                        result = ItemGetServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemGetServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        ItemReplyServerPacket pkt;
                        result = ItemReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemReplyServerPacket_serialize(&pkt, &writer);
                            ItemReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADD:
                    if (direction == 1) {
                        ItemAddServerPacket pkt;
                        result = ItemAddServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemAddServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 1) {
                        ItemRemoveServerPacket pkt;
                        result = ItemRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OBTAIN:
                    if (direction == 1) {
                        ItemObtainServerPacket pkt;
                        result = ItemObtainServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemObtainServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_KICK:
                    if (direction == 1) {
                        ItemKickServerPacket pkt;
                        result = ItemKickServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemKickServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        ItemAgreeServerPacket pkt;
                        result = ItemAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemAgreeServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        result = ItemSpecServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = ItemSpecServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 1) {
                        ItemAcceptServerPacket pkt;
                        result = ItemAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ItemAcceptServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_BOARD:
            switch (action) {
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        BoardRemoveClientPacket pkt;
                        result = BoardRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BoardRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_CREATE:
                    if (direction == 0) {
                        BoardCreateClientPacket pkt;
                        result = BoardCreateClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BoardCreateClientPacket_serialize(&pkt, &writer);
                            BoardCreateClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        BoardTakeClientPacket pkt;
                        result = BoardTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BoardTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        BoardOpenClientPacket pkt;
                        result = BoardOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BoardOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        BoardOpenServerPacket pkt;
                        result = BoardOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BoardOpenServerPacket_serialize(&pkt, &writer);
                            BoardOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        BoardPlayerServerPacket pkt;
                        result = BoardPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BoardPlayerServerPacket_serialize(&pkt, &writer);
                            BoardPlayerServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_JUKEBOX:
            switch (action) {
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        JukeboxOpenClientPacket pkt;
                        result = JukeboxOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        JukeboxOpenServerPacket pkt;
                        result = JukeboxOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxOpenServerPacket_serialize(&pkt, &writer);
                            JukeboxOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_MSG:
                    if (direction == 0) {
                        JukeboxMsgClientPacket pkt;
                        result = JukeboxMsgClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxMsgClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        JukeboxMsgServerPacket pkt;
                        result = JukeboxMsgServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxMsgServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        JukeboxUseClientPacket pkt;
                        result = JukeboxUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxUseClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        JukeboxUseServerPacket pkt;
                        result = JukeboxUseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxUseServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        JukeboxAgreeServerPacket pkt;
                        result = JukeboxAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxAgreeServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        result = JukeboxReplyServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = JukeboxReplyServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        JukeboxPlayerServerPacket pkt;
                        result = JukeboxPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = JukeboxPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_WARP:
            switch (action) {
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        WarpAcceptClientPacket pkt;
                        result = WarpAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WarpAcceptClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        WarpTakeClientPacket pkt;
                        result = WarpTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WarpTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        WarpPlayerServerPacket pkt;
                        result = WarpPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WarpPlayerServerPacket_serialize(&pkt, &writer);
                            WarpPlayerServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CREATE:
                    if (direction == 1) {
                        WarpCreateServerPacket pkt;
                        result = WarpCreateServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WarpCreateServerPacket_serialize(&pkt, &writer);
                            WarpCreateServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REQUEST:
                    if (direction == 1) {
                        WarpRequestServerPacket pkt;
                        result = WarpRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WarpRequestServerPacket_serialize(&pkt, &writer);
                            WarpRequestServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        WarpAgreeServerPacket pkt;
                        result = WarpAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = WarpAgreeServerPacket_serialize(&pkt, &writer);
                            WarpAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_PAPERDOLL:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        PaperdollRequestClientPacket pkt;
                        result = PaperdollRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PaperdollRequestClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        PaperdollRemoveClientPacket pkt;
                        result = PaperdollRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PaperdollRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        PaperdollRemoveServerPacket pkt;
                        result = PaperdollRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PaperdollRemoveServerPacket_serialize(&pkt, &writer);
                            PaperdollRemoveServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADD:
                    if (direction == 0) {
                        PaperdollAddClientPacket pkt;
                        result = PaperdollAddClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PaperdollAddClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        PaperdollReplyServerPacket pkt;
                        result = PaperdollReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PaperdollReplyServerPacket_serialize(&pkt, &writer);
                            PaperdollReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PING:
                    if (direction == 1) {
                        PaperdollPingServerPacket pkt;
                        result = PaperdollPingServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PaperdollPingServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        PaperdollAgreeServerPacket pkt;
                        result = PaperdollAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PaperdollAgreeServerPacket_serialize(&pkt, &writer);
                            PaperdollAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_BOOK:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        BookRequestClientPacket pkt;
                        result = BookRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BookRequestClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        BookReplyServerPacket pkt;
                        result = BookReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = BookReplyServerPacket_serialize(&pkt, &writer);
                            BookReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_MESSAGE:
            switch (action) {
                case PACKET_ACTION_PING:
                    if (direction == 0) {
                        result = MessagePingClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = MessagePingClientPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 1) {
                        MessageOpenServerPacket pkt;
                        result = MessageOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = MessageOpenServerPacket_serialize(&pkt, &writer);
                            MessageOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 1) {
                        result = MessageCloseServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = MessageCloseServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 1) {
                        MessageAcceptServerPacket pkt;
                        result = MessageAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = MessageAcceptServerPacket_serialize(&pkt, &writer);
                            MessageAcceptServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PONG:
                    if (direction == 1) {
                        result = MessagePongServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = MessagePongServerPacket_serialize(&writer);
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_PLAYERS:
            switch (action) {
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        PlayersAcceptClientPacket pkt;
                        result = PlayersAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersAcceptClientPacket_serialize(&pkt, &writer);
                            PlayersAcceptClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        result = PlayersRequestClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = PlayersRequestClientPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_LIST:
                    if (direction == 0) {
                        result = PlayersListClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = PlayersListClientPacket_serialize(&writer);
                    }
                    if (direction == 1) {
                        PlayersListServerPacket pkt;
                        result = PlayersListServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersListServerPacket_serialize(&pkt, &writer);
                            PlayersListServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        PlayersReplyServerPacket pkt;
                        result = PlayersReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersReplyServerPacket_serialize(&pkt, &writer);
                            PlayersReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        PlayersAgreeServerPacket pkt;
                        result = PlayersAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersAgreeServerPacket_serialize(&pkt, &writer);
                            PlayersAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 1) {
                        PlayersRemoveServerPacket pkt;
                        result = PlayersRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PING:
                    if (direction == 1) {
                        PlayersPingServerPacket pkt;
                        result = PlayersPingServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersPingServerPacket_serialize(&pkt, &writer);
                            PlayersPingServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PONG:
                    if (direction == 1) {
                        PlayersPongServerPacket pkt;
                        result = PlayersPongServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersPongServerPacket_serialize(&pkt, &writer);
                            PlayersPongServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_NET242:
                    if (direction == 1) {
                        PlayersNet242ServerPacket pkt;
                        result = PlayersNet242ServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayersNet242ServerPacket_serialize(&pkt, &writer);
                            PlayersNet242ServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_DOOR:
            switch (action) {
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        DoorOpenClientPacket pkt;
                        result = DoorOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = DoorOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        DoorOpenServerPacket pkt;
                        result = DoorOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = DoorOpenServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 1) {
                        DoorCloseServerPacket pkt;
                        result = DoorCloseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = DoorCloseServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_CHEST:
            switch (action) {
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        ChestOpenClientPacket pkt;
                        result = ChestOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        ChestOpenServerPacket pkt;
                        result = ChestOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestOpenServerPacket_serialize(&pkt, &writer);
                            ChestOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADD:
                    if (direction == 0) {
                        ChestAddClientPacket pkt;
                        result = ChestAddClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestAddClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        ChestTakeClientPacket pkt;
                        result = ChestTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        ChestReplyServerPacket pkt;
                        result = ChestReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestReplyServerPacket_serialize(&pkt, &writer);
                            ChestReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_GET:
                    if (direction == 1) {
                        ChestGetServerPacket pkt;
                        result = ChestGetServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestGetServerPacket_serialize(&pkt, &writer);
                            ChestGetServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        ChestAgreeServerPacket pkt;
                        result = ChestAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestAgreeServerPacket_serialize(&pkt, &writer);
                            ChestAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        result = ChestSpecServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = ChestSpecServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 1) {
                        ChestCloseServerPacket pkt;
                        result = ChestCloseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ChestCloseServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_REFRESH:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        result = RefreshRequestClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = RefreshRequestClientPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        RefreshReplyServerPacket pkt;
                        result = RefreshReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RefreshReplyServerPacket_serialize(&pkt, &writer);
                            RefreshReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_RANGE:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        RangeRequestClientPacket pkt;
                        result = RangeRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RangeRequestClientPacket_serialize(&pkt, &writer);
                            RangeRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        RangeReplyServerPacket pkt;
                        result = RangeReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RangeReplyServerPacket_serialize(&pkt, &writer);
                            RangeReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_PLAYER_RANGE:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        PlayerRangeRequestClientPacket pkt;
                        result = PlayerRangeRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PlayerRangeRequestClientPacket_serialize(&pkt, &writer);
                            PlayerRangeRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_NPC_RANGE:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        NpcRangeRequestClientPacket pkt;
                        result = NpcRangeRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcRangeRequestClientPacket_serialize(&pkt, &writer);
                            NpcRangeRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_PARTY:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        PartyRequestClientPacket pkt;
                        result = PartyRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyRequestClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        PartyRequestServerPacket pkt;
                        result = PartyRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyRequestServerPacket_serialize(&pkt, &writer);
                            PartyRequestServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        PartyAcceptClientPacket pkt;
                        result = PartyAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyAcceptClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        PartyRemoveClientPacket pkt;
                        result = PartyRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        PartyRemoveServerPacket pkt;
                        result = PartyRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        PartyTakeClientPacket pkt;
                        result = PartyTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyTakeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        PartyReplyServerPacket pkt;
                        result = PartyReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyReplyServerPacket_serialize(&pkt, &writer);
                            PartyReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CREATE:
                    if (direction == 1) {
                        PartyCreateServerPacket pkt;
                        result = PartyCreateServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyCreateServerPacket_serialize(&pkt, &writer);
                            PartyCreateServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADD:
                    if (direction == 1) {
                        PartyAddServerPacket pkt;
                        result = PartyAddServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyAddServerPacket_serialize(&pkt, &writer);
                            PartyAddServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 1) {
                        result = PartyCloseServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = PartyCloseServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_LIST:
                    if (direction == 1) {
                        PartyListServerPacket pkt;
                        result = PartyListServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyListServerPacket_serialize(&pkt, &writer);
                            PartyListServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        PartyAgreeServerPacket pkt;
                        result = PartyAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyAgreeServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TARGET_GROUP:
                    if (direction == 1) {
                        PartyTargetGroupServerPacket pkt;
                        result = PartyTargetGroupServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PartyTargetGroupServerPacket_serialize(&pkt, &writer);
                            PartyTargetGroupServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_GUILD:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        GuildRequestClientPacket pkt;
                        result = GuildRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildRequestClientPacket_serialize(&pkt, &writer);
                            GuildRequestClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        GuildRequestServerPacket pkt;
                        result = GuildRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildRequestServerPacket_serialize(&pkt, &writer);
                            GuildRequestServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        GuildAcceptClientPacket pkt;
                        result = GuildAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildAcceptClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        GuildAcceptServerPacket pkt;
                        result = GuildAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildAcceptServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        GuildRemoveClientPacket pkt;
                        result = GuildRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 0) {
                        GuildAgreeClientPacket pkt;
                        result = GuildAgreeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildAgreeClientPacket_serialize(&pkt, &writer);
                            GuildAgreeClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        GuildAgreeServerPacket pkt;
                        result = GuildAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildAgreeServerPacket_serialize(&pkt, &writer);
                            GuildAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_CREATE:
                    if (direction == 0) {
                        GuildCreateClientPacket pkt;
                        result = GuildCreateClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildCreateClientPacket_serialize(&pkt, &writer);
                            GuildCreateClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        GuildCreateServerPacket pkt;
                        result = GuildCreateServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildCreateServerPacket_serialize(&pkt, &writer);
                            GuildCreateServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 0) {
                        GuildPlayerClientPacket pkt;
                        result = GuildPlayerClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildPlayerClientPacket_serialize(&pkt, &writer);
                            GuildPlayerClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_TAKE:
                    if (direction == 0) {
                        GuildTakeClientPacket pkt;
                        result = GuildTakeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildTakeClientPacket_serialize(&pkt, &writer);
                            GuildTakeClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        GuildTakeServerPacket pkt;
                        result = GuildTakeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildTakeServerPacket_serialize(&pkt, &writer);
                            GuildTakeServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        GuildUseClientPacket pkt;
                        result = GuildUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildUseClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_BUY:
                    if (direction == 0) {
                        GuildBuyClientPacket pkt;
                        result = GuildBuyClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildBuyClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        GuildBuyServerPacket pkt;
                        result = GuildBuyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildBuyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        GuildOpenClientPacket pkt;
                        result = GuildOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        GuildOpenServerPacket pkt;
                        result = GuildOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildOpenServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TELL:
                    if (direction == 0) {
                        GuildTellClientPacket pkt;
                        result = GuildTellClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildTellClientPacket_serialize(&pkt, &writer);
                            GuildTellClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        GuildTellServerPacket pkt;
                        result = GuildTellServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildTellServerPacket_serialize(&pkt, &writer);
                            GuildTellServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPORT:
                    if (direction == 0) {
                        GuildReportClientPacket pkt;
                        result = GuildReportClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildReportClientPacket_serialize(&pkt, &writer);
                            GuildReportClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        GuildReportServerPacket pkt;
                        result = GuildReportServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildReportServerPacket_serialize(&pkt, &writer);
                            GuildReportServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_JUNK:
                    if (direction == 0) {
                        GuildJunkClientPacket pkt;
                        result = GuildJunkClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildJunkClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_KICK:
                    if (direction == 0) {
                        GuildKickClientPacket pkt;
                        result = GuildKickClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildKickClientPacket_serialize(&pkt, &writer);
                            GuildKickClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        result = GuildKickServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = GuildKickServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_RANK:
                    if (direction == 0) {
                        GuildRankClientPacket pkt;
                        result = GuildRankClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildRankClientPacket_serialize(&pkt, &writer);
                            GuildRankClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        GuildRankServerPacket pkt;
                        result = GuildRankServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildRankServerPacket_serialize(&pkt, &writer);
                            GuildRankServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        GuildReplyServerPacket pkt;
                        result = GuildReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildReplyServerPacket_serialize(&pkt, &writer);
                            GuildReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_SELL:
                    if (direction == 1) {
                        GuildSellServerPacket pkt;
                        result = GuildSellServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = GuildSellServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_SPELL:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        SpellRequestClientPacket pkt;
                        result = SpellRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellRequestClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        SpellRequestServerPacket pkt;
                        result = SpellRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellRequestServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TARGET_SELF:
                    if (direction == 0) {
                        SpellTargetSelfClientPacket pkt;
                        result = SpellTargetSelfClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellTargetSelfClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        SpellTargetSelfServerPacket pkt;
                        result = SpellTargetSelfServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellTargetSelfServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TARGET_OTHER:
                    if (direction == 0) {
                        SpellTargetOtherClientPacket pkt;
                        result = SpellTargetOtherClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellTargetOtherClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        SpellTargetOtherServerPacket pkt;
                        result = SpellTargetOtherServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellTargetOtherServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TARGET_GROUP:
                    if (direction == 0) {
                        SpellTargetGroupClientPacket pkt;
                        result = SpellTargetGroupClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellTargetGroupClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        SpellTargetGroupServerPacket pkt;
                        result = SpellTargetGroupServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellTargetGroupServerPacket_serialize(&pkt, &writer);
                            SpellTargetGroupServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        SpellUseClientPacket pkt;
                        result = SpellUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellUseClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        SpellPlayerServerPacket pkt;
                        result = SpellPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ERROR:
                    if (direction == 1) {
                        result = SpellErrorServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = SpellErrorServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        SpellReplyServerPacket pkt;
                        result = SpellReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = SpellReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_TRADE:
            switch (action) {
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        TradeRequestClientPacket pkt;
                        result = TradeRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeRequestClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        TradeRequestServerPacket pkt;
                        result = TradeRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeRequestServerPacket_serialize(&pkt, &writer);
                            TradeRequestServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        TradeAcceptClientPacket pkt;
                        result = TradeAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeAcceptClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 0) {
                        TradeRemoveClientPacket pkt;
                        result = TradeRemoveClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeRemoveClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 0) {
                        TradeAgreeClientPacket pkt;
                        result = TradeAgreeClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeAgreeClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        TradeAgreeServerPacket pkt;
                        result = TradeAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeAgreeServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ADD:
                    if (direction == 0) {
                        TradeAddClientPacket pkt;
                        result = TradeAddClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeAddClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_CLOSE:
                    if (direction == 0) {
                        result = TradeCloseClientPacket_deserialize(&reader);
                        if (result != -1)
                            result = TradeCloseClientPacket_serialize(&writer);
                    }
                    if (direction == 1) {
                        TradeCloseServerPacket pkt;
                        result = TradeCloseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeCloseServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 1) {
                        TradeOpenServerPacket pkt;
                        result = TradeOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeOpenServerPacket_serialize(&pkt, &writer);
                            TradeOpenServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        TradeReplyServerPacket pkt;
                        result = TradeReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeReplyServerPacket_serialize(&pkt, &writer);
                            TradeReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADMIN:
                    if (direction == 1) {
                        TradeAdminServerPacket pkt;
                        result = TradeAdminServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeAdminServerPacket_serialize(&pkt, &writer);
                            TradeAdminServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 1) {
                        TradeUseServerPacket pkt;
                        result = TradeUseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeUseServerPacket_serialize(&pkt, &writer);
                            TradeUseServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        TradeSpecServerPacket pkt;
                        result = TradeSpecServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = TradeSpecServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_QUEST:
            switch (action) {
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        QuestUseClientPacket pkt;
                        result = QuestUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = QuestUseClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        QuestAcceptClientPacket pkt;
                        result = QuestAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = QuestAcceptClientPacket_serialize(&pkt, &writer);
                            QuestAcceptClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_LIST:
                    if (direction == 0) {
                        QuestListClientPacket pkt;
                        result = QuestListClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = QuestListClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        QuestListServerPacket pkt;
                        result = QuestListServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = QuestListServerPacket_serialize(&pkt, &writer);
                            QuestListServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPORT:
                    if (direction == 1) {
                        QuestReportServerPacket pkt;
                        result = QuestReportServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = QuestReportServerPacket_serialize(&pkt, &writer);
                            QuestReportServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_DIALOG:
                    if (direction == 1) {
                        QuestDialogServerPacket pkt;
                        result = QuestDialogServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = QuestDialogServerPacket_serialize(&pkt, &writer);
                            QuestDialogServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_MARRIAGE:
            switch (action) {
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        MarriageOpenClientPacket pkt;
                        result = MarriageOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = MarriageOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        MarriageOpenServerPacket pkt;
                        result = MarriageOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = MarriageOpenServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        MarriageRequestClientPacket pkt;
                        result = MarriageRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = MarriageRequestClientPacket_serialize(&pkt, &writer);
                            MarriageRequestClientPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        MarriageReplyServerPacket pkt;
                        result = MarriageReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = MarriageReplyServerPacket_serialize(&pkt, &writer);
                            MarriageReplyServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_PRIEST:
            switch (action) {
                case PACKET_ACTION_ACCEPT:
                    if (direction == 0) {
                        PriestAcceptClientPacket pkt;
                        result = PriestAcceptClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PriestAcceptClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_OPEN:
                    if (direction == 0) {
                        PriestOpenClientPacket pkt;
                        result = PriestOpenClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PriestOpenClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    if (direction == 1) {
                        PriestOpenServerPacket pkt;
                        result = PriestOpenServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PriestOpenServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REQUEST:
                    if (direction == 0) {
                        PriestRequestClientPacket pkt;
                        result = PriestRequestClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PriestRequestClientPacket_serialize(&pkt, &writer);
                            PriestRequestClientPacket_free(&pkt);
                        }
                    }
                    if (direction == 1) {
                        PriestRequestServerPacket pkt;
                        result = PriestRequestServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PriestRequestServerPacket_serialize(&pkt, &writer);
                            PriestRequestServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 0) {
                        PriestUseClientPacket pkt;
                        result = PriestUseClientPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PriestUseClientPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        PriestReplyServerPacket pkt;
                        result = PriestReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = PriestReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_AVATAR:
            switch (action) {
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        AvatarReplyServerPacket pkt;
                        result = AvatarReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AvatarReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REMOVE:
                    if (direction == 1) {
                        AvatarRemoveServerPacket pkt;
                        result = AvatarRemoveServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AvatarRemoveServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        AvatarAgreeServerPacket pkt;
                        result = AvatarAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AvatarAgreeServerPacket_serialize(&pkt, &writer);
                            AvatarAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADMIN:
                    if (direction == 1) {
                        AvatarAdminServerPacket pkt;
                        result = AvatarAdminServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = AvatarAdminServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_EFFECT:
            switch (action) {
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        EffectPlayerServerPacket pkt;
                        result = EffectPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EffectPlayerServerPacket_serialize(&pkt, &writer);
                            EffectPlayerServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 1) {
                        EffectUseServerPacket pkt;
                        result = EffectUseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EffectUseServerPacket_serialize(&pkt, &writer);
                            EffectUseServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        EffectAgreeServerPacket pkt;
                        result = EffectAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EffectAgreeServerPacket_serialize(&pkt, &writer);
                            EffectAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_TARGET_OTHER:
                    if (direction == 1) {
                        EffectTargetOtherServerPacket pkt;
                        result = EffectTargetOtherServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EffectTargetOtherServerPacket_serialize(&pkt, &writer);
                            EffectTargetOtherServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPORT:
                    if (direction == 1) {
                        result = EffectReportServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = EffectReportServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        EffectSpecServerPacket pkt;
                        result = EffectSpecServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EffectSpecServerPacket_serialize(&pkt, &writer);
                            EffectSpecServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ADMIN:
                    if (direction == 1) {
                        EffectAdminServerPacket pkt;
                        result = EffectAdminServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = EffectAdminServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_NPC:
            switch (action) {
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        NpcAgreeServerPacket pkt;
                        result = NpcAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcAgreeServerPacket_serialize(&pkt, &writer);
                            NpcAgreeServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        NpcReplyServerPacket pkt;
                        result = NpcReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        NpcSpecServerPacket pkt;
                        result = NpcSpecServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcSpecServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 1) {
                        NpcAcceptServerPacket pkt;
                        result = NpcAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcAcceptServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_JUNK:
                    if (direction == 1) {
                        NpcJunkServerPacket pkt;
                        result = NpcJunkServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcJunkServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        NpcPlayerServerPacket pkt;
                        result = NpcPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcPlayerServerPacket_serialize(&pkt, &writer);
                            NpcPlayerServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_DIALOG:
                    if (direction == 1) {
                        NpcDialogServerPacket pkt;
                        result = NpcDialogServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = NpcDialogServerPacket_serialize(&pkt, &writer);
                            NpcDialogServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_CAST:
            switch (action) {
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        CastReplyServerPacket pkt;
                        result = CastReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CastReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        CastSpecServerPacket pkt;
                        result = CastSpecServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CastSpecServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 1) {
                        CastAcceptServerPacket pkt;
                        result = CastAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = CastAcceptServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_ARENA:
            switch (action) {
                case PACKET_ACTION_DROP:
                    if (direction == 1) {
                        result = ArenaDropServerPacket_deserialize(&reader);
                        if (result != -1)
                            result = ArenaDropServerPacket_serialize(&writer);
                    }
                    break;
                case PACKET_ACTION_USE:
                    if (direction == 1) {
                        ArenaUseServerPacket pkt;
                        result = ArenaUseServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ArenaUseServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_SPEC:
                    if (direction == 1) {
                        ArenaSpecServerPacket pkt;
                        result = ArenaSpecServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ArenaSpecServerPacket_serialize(&pkt, &writer);
                            ArenaSpecServerPacket_free(&pkt);
                        }
                    }
                    break;
                case PACKET_ACTION_ACCEPT:
                    if (direction == 1) {
                        ArenaAcceptServerPacket pkt;
                        result = ArenaAcceptServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = ArenaAcceptServerPacket_serialize(&pkt, &writer);
                            ArenaAcceptServerPacket_free(&pkt);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_RECOVER:
            switch (action) {
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        RecoverPlayerServerPacket pkt;
                        result = RecoverPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RecoverPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_AGREE:
                    if (direction == 1) {
                        RecoverAgreeServerPacket pkt;
                        result = RecoverAgreeServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RecoverAgreeServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_LIST:
                    if (direction == 1) {
                        RecoverListServerPacket pkt;
                        result = RecoverListServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RecoverListServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_REPLY:
                    if (direction == 1) {
                        RecoverReplyServerPacket pkt;
                        result = RecoverReplyServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RecoverReplyServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
                case PACKET_ACTION_TARGET_GROUP:
                    if (direction == 1) {
                        RecoverTargetGroupServerPacket pkt;
                        result = RecoverTargetGroupServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = RecoverTargetGroupServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
        case PACKET_FAMILY_MUSIC:
            switch (action) {
                case PACKET_ACTION_PLAYER:
                    if (direction == 1) {
                        MusicPlayerServerPacket pkt;
                        result = MusicPlayerServerPacket_deserialize(&pkt, &reader);
                        if (result != -1) {
                            result = MusicPlayerServerPacket_serialize(&pkt, &writer);
                        }
                    }
                    break;
            }
            break;
    }

    if (result != -1 && writer.length > 0 &&
        writer.length <= sizeof(g_roundtrip_buf)) {
        memcpy(g_roundtrip_buf, writer.data, writer.length);
        g_roundtrip_len = (int)writer.length;
    }
    free(writer.data);
    return g_roundtrip_len > 0 ? g_roundtrip_buf : NULL;
}
